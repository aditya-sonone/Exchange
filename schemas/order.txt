enum Side {
    Buy,
    Sell
}

enum OrderType {
    Stop,
    Limit,
    IOC, 
    FOK
}

@packet(1)
struct Order {
    uint64 orderId;
    Side side;
    uint64 price;
}

struct PacketHeader {
    uint16 packetId;
    uint32 payloadSize;
}